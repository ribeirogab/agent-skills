# Broken Layout — README

This README.md should not exist inside the skill folder. Its presence is a structural violation per Section 2 of the canonical audit checklist (no `README.md` directly inside the skill folder).

A correct audit must flag this file. Deletion is a destructive operation, so the auditor should classify it as `High` regression risk and surface it in the final report rather than deleting it autonomously.
