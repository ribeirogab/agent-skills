# Deep Doc

This file lives at `references/nested/deep-doc.md` — two levels deep from `SKILL.md`. Per Anthropic's authoring rules, references must be exactly one level deep so the runtime can read complete files instead of partial-reading nested ones with `head -100`.

A correct audit must flag this as a Structural finding under Section 3 of the canonical checklist.
